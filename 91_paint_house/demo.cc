#include <iostream>
#include <vector>

using namespace std;


class Solution {
public:
	/**
	 * Multi-step multi-choices per step -> recursion (with memoization) or dp.
	 */
    int minCost(vector<vector<int>>& costs)
	{

    }
};
