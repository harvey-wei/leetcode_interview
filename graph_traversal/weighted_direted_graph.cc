#include <iostream>
#include <vector>

class DirectedGraph
{
    private:
        /* Graph vertcies are numbered from 0 to N-1 with N as the total count of vertcies. */
        /* adj[i] stores the neighbors of vertex i */
        /* weights has the same shape as adj */
        std::vector<std::vector<int>> adj;

        std::vector<std::vector<int>> weights;



};

int
main()
{
    return 0;
}
